import React from "react";
import basicStyles from "../ModelFormStyles.module.css";

const ModelUpdateForm = ({modelId}) => {

    return (
        <div className={basicStyles.modelFormContainer}>
            MODEL CREATE FORM
        </div>
    );
};

export default ModelUpdateForm;